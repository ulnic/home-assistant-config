nzb_toggle:
  alias: '[HOME] NZB Toggle'
  mode: restart
  sequence:
    - service_template: >
        {% if is_state('sensor.nzbget_download_paused' , 'True') %}
          nzbget.resume
        {% else %}
          nzbget.pause
        {% endif %}
